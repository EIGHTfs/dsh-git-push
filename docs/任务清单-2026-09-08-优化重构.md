# dsh-git-push 优化重构任务清单（2026-09-08）

- 主持人：GLM（glm-5.3-flash）× DeepSeek Harness
- 输入：五份分析报告（GLM 66.5 / mimo 54 / agnes 86 / deepseek-v4-flash 54 / Ornith-1.5-9B-Q8_0 81）+ `docs/1.js` 质量扫描参考 + 用户指示五条
- 用户指示（2026-09-08）：
  1. 参考工作区 doc 目录多份分析文档与 `1.js` 做代码质量扫描
  2. 拦截措辞用外部 JSON 储存；此 JSON 用注释豁免审计；豁免审计只豁免「隐私入库」类，不是全都豁免
  3. 取消代码中所有与 dsh-git-push-User 的关联；token/sshkey 存插件自己配置文件；repo-index JSON 也存插件目录
  4. User 仓内容：工作流 skill → 技能仓库 ai-work-archive；其余必要内容 → 插件；然后废除 User 仓
  5. 审计功能规则插件化：规则可外挂（别人写的审计功能与配套规则可引入）；当前规则归属 EIGHTfs，代码用读配置指向这份规则
- 记号约定：`[ ]` 未做 / `[x]` 完成 / `[~]` 部分完成（备注原因）。每完成一步立即在本文件打勾。

## A 组 · 安全与健壮性止血（四份报告共同 P0）

- [x] A1 package.json：补 peerDependencies（@deepseek-ai/dsh-tools / dsh-llm / schemastery / client 依赖）/ engines（node>=18）/ scripts（test）/ files 补 template
- [x] A2 `findGitDirs` 由 `execSync` 字符串拼接改 `spawnSync('find', [args...])` 数组参数，消灭 shell 注入面
- [x] A3 `runGit`/`gitRaw` 加 try/catch + maxBuffer 兜底，git 缺失/输出超限时返回结构化错误不抛
- [x] A4 origin remote URL 脱敏：新增 `maskRemoteUrl`，`readRepoStatus`/scan 回传前抹掉内嵌 token
- [x] A5 `readJson` 加 body 大小上限（5MB，超限断开并回结构化错误）；`getDiff` untracked 文件跳过二进制/大文件
- [x] A6 `.gitignore` 移除 `docs/code-quality-checklist.yaml` 忽略行（「YAML 非法」系误判）+ yaml `dimensions_weight` 10 项空值填实（权重与 quality.js DEFAULT_WEIGHTS 对齐，总和 100）

## B 组 · 去 User 仓依赖（用户指示 3/4）

- [x] B1 凭据迁移：token / SSH 私钥·公钥改存插件配置目录（`credentialsDir()` = DSH_HOME/git-push → HOME/.dsh/git-push 兜底，token 0600，.gitignore 拦截），探测顺序「插件配置目录 > 显式 tokenPath > 项目内 .git-push-token > 历史 data/sensitive」
- [x] B2 `dsh-repo-index.json` / `tools-index.md` 改存插件配置目录（credentialsDir()），同步目标去 User 仓化
- [~] B3 User 仓 16 个工作流 skill md 迁移——技能仓库 ai-work-archive 挂载只读（EROFS）不可写，按 skill-repo-index「插件相关 skill 唯一位置 = 插件项目 skills/」迁入 `skills/git-workflow-gitpush/`（含来源 README）；requirements.md 内置为 lib/user-requirements.json；dsh-repo-index.md（已废弃）与凭据类文件不迁移
- [x] B4 requirements 门禁改读插件内置清单（`lib/user-requirements.json` 与原 requirements.md 七条逐字一致 + `<配置目录>/requirements.json` 可覆盖），去同级仓依赖
- [x] B5 删除 core/index 全部 User 仓关联代码（USER_REPO_*/resolveUserDir/userRepoCandidates/inspectUserRepo/scoreUserRepo/pickBestUserRepo/looksLikeUserRepo/spawnGit/loadUserRequirements/ensureUserRepoSibling/createUserRepoTemplate/resolveUserSshKey 共 13 符号 + template/user-repo 模板；cloneViaApi/probeTokenValid 保留）
- [x] B6 skills 注入改造：插件 skills/ + 技能仓库 ai-work-archive/skills（git-* 散文档 + git-workflow-gitpush/ 子目录）双来源（`resolveSkillRepoDirs`），替代 User 仓注入

## C 组 · 审计规则插件化（用户指示 2/5 + 1.js 吸收）

- [x] C1 设计审计规则包格式（JSON：owner / version / rules[]，rule={id,kind,level,pattern|threshold,message}，kind ∈ secret|credential-file|comment-wording|doc-conversation|code-quality），规则包可来自内置/本地文件/URL
- [x] C2 当前规则集（归属 EIGHTfs）落盘 `lib/audit-rules/eightfs.rules.json`，代码零硬编码规则
- [x] C3 audit.js 改规则引擎：配置 `auditRuleset`（{source: builtin|file|url, path?, url?}）装载规则包，校验 + 降级内置；code_audit 工具出 `ruleset` 参数（default|eightfs|自定义路径）
- [~] C4 吸收 `1.js` 规则（文件长度 ≤400 / 变量名 ≥2 / 注释占比 5%-30% / 函数名 camelCase）进规则集 code-quality kind——规则包 codeQuality 桶已就绪（id 归桶编译），4 个风格检查器待下一批接入 quality.js
- [x] C5 措辞规则独立 JSON（并入规则包 comment-wording 桶，可整包替换）+「JSON 文件注释豁免」：`.json` 内 `//` 注释行豁免隐私入库类（secret/credential-file/credential-ref）；comment-wording/doc-conversation/代码质量不豁免（audit.js isJsonCommentLine + JSONC 解析容忍）
- [x] C6 code_audit 输出增加规则包来源/归属信息（ruleset: {name, owner, version, source}）
- [x] C7 评分器诚实化（Ornith 报告独家发现）：scoreQuality 其余 6 维硬编码常量（8/9/7/7/7/7）改为「有证据则实算，无证据标注基准值」并可由规则包/配置调整；质量输出声明哪些维度是实测、哪些是基准

## D 组 · 结构与性能优化（报告 P1/P2）

- [x] D1 拆 index.js apply()：registerHttpRoutes / registerTools / registerSettings / registerEnvInject / registerEvents 分函数，apply 只做编排（批次3 落地为文件级拆分：apply 瘦身为调度器，按功能拆 plugin-config/plugin-setup/plugin-context-inject/repo-index-sync/plugin-audit/plugin-commit-flow/plugin-push-permit/plugin-http/plugin-tools 九模块；core.js 2511 行同步拆 11 模块 + 门面，导出面零变化；index.js/core.js 函数清零（apply 1183→119 行，core 2511→37 行）；残留 10 项在新增 plugin-* 的 register* 编排函数（内嵌工具/HTTP 闭包按嵌套计数）+ viewer.js w68，列批次4；顺带修复 v1.41.0 引入的 log TDZ 启动必崩雷 + rebuildHistory 分支提交身份兜底）
- [ ] D2 静默 catch 治理：核心路径 catch 补 ctx.logger.warn；checkSilentCatch 支持 `catch {` 多行形态识别
- [ ] D3 性能：getCommitHistory 批量化（numstat 一次拉全量替代逐文件 spawn）；scanRepos/resolveViewerRepo 60s 缓存
- [ ] D4 HTTP API 防护：gen-readme writePath 限制在 workspaceRoot 内；rebuild force 需 confirm:true；POST 端点要求同源头
- [x] D5 质量规则支持全量扫描模式（auditFullScan，同 hardcodeFullScan 开关）（批次4 落地 v1.43.0：`lib/full-scan.js` 全仓 AI 对话残留注释扫描引擎——分数制，规则包 `fullScan` 段黑名单加分/白名单减分/阈值；只读不删码，报告按分数降序列表格；接入 `audit_full_scan` 工具 + `GET /api/git-push/full-scan` + 提交审计新增行注释 full-scan warning；test-full-scan 26/0）
- [ ] D6 viewer 内嵌 280 行 IIFE 脚本抽出为可测模块并补最低限度测试（Ornith 报告发现）
- [x] D7 默认 owner `EIGHTfs` 硬编码改可配置（`config.githubOwner` + `setDefaultGithubOwner/getDefaultGithubOwner`，缺省保留现值）

## E 组 · 测试、文档、发布

- [ ] E1 测试修复：test-core 夹具封闭到临时目录（不再命中真实同级仓）、test-apply 可运行、全部测试通过
- [~] E2 版本号 v1.40.0 + README 版本表已同步（架构描述/目录表/配置键/1.40.0 行）；「规则包格式说明」待 C 组完成后补入（批次3 已同步至 v1.42.0：目录表补齐 20 个 lib 模块 + 1.42.0 版本行）
- [ ] E3 提交推送（逐条核对 requirements 七条 + remoteHeads 表格回传）

### G 组：独立化与命名（批次5 v1.44.0 已落地 G1；更名待用户确认）
- [x] G1 独立 CLI：`cli.mjs` 五命令（audit/full-scan/commit/scan/ruleset）零依赖脱离 DSH 调用纯引擎；package.json `bin: git-sluice`；test-cli 8 断言
- [x] G3 autoClean 全链废除（v1.45.0）：只警告不删改总原则确立；提交审计扫提交新增行、fullScan 扫全仓
- [ ] G2 正式更名 dsh-git-push → git-sluice（候选已提出：git-sluice 主推 / git-reeve、git-bailiff、git-seneschal 备选；npm 全部空闲、GitHub 撞名 ≤5）——涉及 package.json name / 仓名 / cordis 安装标识 / README / dsh-repo-index，需用户拍板后一次改齐
### F 组：设置侧边栏拓展（v1.44.0 计划，D1 重构已完成可开工）
- [x] F1 设置侧边栏独立页（v1.46.0）：`settings.section` 槽注册，侧边栏出 Git 提交推送页，复用插件配置卡片同一 controller/store；四卡（权重/强度/规则包）待 F2-F4
- [ ] F2 自定义权重设置：quality 评分维度权重 + fileLinesBase/fileKbBase 等文件级阈值可视配置（写 config，规则包优先）
- [ ] F3 审计强度开关：blockOn（none/blocker/any）+ hardcodeFullScan + 附属能力 fullScan 开关（提交时是否跑新增行注释评分）
- [ ] F4 规则包管理：导入（本地文件/在线 URL 拉取写入配置目录）/ 导出（当前生效包 JSON 下载）/ 删除（回退 builtin）+ 包来源溯源展示
- [ ] F5 插件配置保留：现有设置 → 插件 → 插件配置（token/owner/深度等）原样保留，新增卡不覆盖旧字段
## 执行记录

- 2026-09-08 · 批次7（凭据迁移）：v1.46.0 推送 401 排查——**v1.40.0 凭据目录迁移后旧位置凭据没搬过来**：插件配置目录 `.dsh-home/.dsh/git-push/` 只有 tools-index.md，github-token 与 id_ed25519 仍在旧同级仓 `工作区/dsh-git-push-User/`；v1.39.0 读旧位置所以此前推送正常，换装 1.46.0 后只读新位置 → API 401 Bad credentials + SSH 回退无私钥。已把两者 cp 到新目录（0600）；token 实测 api.github.com/user 返回 login=EIGHTfs 有效，SSH 公钥未在 GitHub 绑定（permission denied，不影响 API 推送）。待办：设置页卡片应加「凭据迁移提示」——读不到 token 时提示从旧同级仓位置搬运，或检测旧路径存在并一键迁移
- 2026-09-08 · 批次7（F1）：**设置侧边栏独立页 v1.46.0**——根因排查：git-push 侧边栏从未出现（用户实测「skill 记分榜有、git-push 没有」）因两者挂的槽位不同：记分榜挂 `settings.section`（侧边栏导航页），git-push 只挂 `settings.plugin.item`（插件配置卡片位）；settings 数据层 `ctx.settings.register()` 一直在（settings.yaml 第 89 行 `git-push:` section 实证存在）。实现：client.js apply 内加 `settings.section` 注册，渲染包装组件内嵌 `GitPushCard`；卡组件吃 `t`/`useGitPushCard`/`check` 三项，其中 SnapshotStore 是裸 observable（无 selector hook，hook 合成本是 ui-renderer 的 uSES 桥）故自建 `react.useSyncExternalStore(store.subscribe, store.getSnapshot)` 补 hook、自取模块级 `zh` 表补 `t`；controller 单例两处共享，卡片与页面状态一致；原 plugin.item 卡片保留（F5 插件配置保留）；client 改动需重启生效
- 2026-09-08 · 批次7（缺陷修复）：v1.45.0 删除 autoClean 99 行时误伤 docs-conversation 的 skill 署名豁免正则——丢失 `用户原话|` 前缀致 test-audit 4 项挂；补回并去 docstring 重复行。另给 test-full-scan.mjs 加 `dsh-skip-sensitive` 文件头（第 2 行，命中 hasFileHeaderExempt 前 3 行判定）+ 夹具复原（`用户原话：`/`# 用户说`）：旧实例 autoClean 三次篡改测试夹具的根因是测试文件无豁免头，现与 test-audit 同规则免疫
- 2026-09-08 · 批次7（部署教训）：重启加载的是**安装副本**（`.dsh-home/.dsh/profiles/web/node_modules/dsh-git-push`），不是工作区仓——工作区改动必须 rsync 到安装副本才生效；本轮发现重启后安装副本仍 1.39.0（autoClean 活着）才 rsync 1.45.0 后重启。验证口径：探测 1.45.0 新增端点 `/api/git-push/full-scan` 返回 `缺少 repo` 即证明新代码已加载
- 2026-09-08 · 批次6（用户指示「自动清理应该舍弃了，都是警告；区别是一个扫提交、一个全量扫描」）：**autoClean 全链废除 v1.45.0**——实锤第 3 次事故（批次4 test-full-scan 夹具 `# 用户说…`/`* 用户原话…` 被 autoClean 静默改写致测试挂 3 项，commitStartOf 字符串不感知是根因）；确立总原则「**只警告、不删改**」：提交审计扫提交新增行、fullScan 扫全仓，两通道皆只出警告报告；移除 autoCleanCommentWording（plugin-audit）/cleanCommentWording+WORDING_REWRITES（audit.js -99 行）/commit-flow autoClean 分支/index 传参/孤儿 import，措辞 finding message 去「提交时自动清理」过期承诺；test-audit 清 11 清理函数用例改审计语义（76/0），test-full-scan 夹具恢复+收尾行回归断言（27/0），full-scan 顺修块注释 `*/` 纯闭合行产出垃圾文本 bug；README/技能说明书/任务清单同步
- 2026-09-08 · 批次5（G1）：独立 CLI v1.44.0——用户新需求「脱离插件调用」：核实引擎层（v1.42.0 拆分产物）零 DSH 依赖（真 import @deepseek-ai/* 仅 4 文件且全在接线层，llm.js 无需惰性改造），新增 cli.mjs（audit/full-scan/commit/scan/ruleset 五命令 + --json/--ruleset/--push/--req-confirm/--fail-on-warn，退出码 0/1/2/3，零依赖手写 argv 解析），bin: git-sluice；修 loadBuiltinRulePack {ok,pack} 解包坑；test-cli 8 断言（含 tmp 仓端到端：门禁拒绝退出 2 / --req-confirm 放行真实落库）；更名候选提出：git-sluice（主推，npm 空闲/GitHub 撞名 2，水闸隐喻：审计=闸 push=放流）/git-reeve/git-bailiff/git-seneschal 备选
- 2026-09-08 · 批次4（D5）：全仓 AI 对话残留注释扫描（附属能力 v1.43.0）——用户确立分数制：规则包 `fullScan` 段（黑名单 keywords 加分/白名单 protectWords 减分/threshold，两份内置包已配）+ `lib/full-scan.js` 引擎（extractComments 行锚定提取修复字符串 URL 误报 / scoreComment 黑加白减 / fullScanRepo 只读全仓 markdown 表格按分降序 / listTextFiles 跳 node_modules·snapshots·vendor 等）；接入三处：`audit_full_scan` 工具（plugin-tools）+ `GET /api/git-push/full-scan`（plugin-http）+ 提交审计新增行注释评分（audit.js checkFullScanLines，warning 不拦截不删码）；rule-packs 校验+编译 fullScan 段（非法正则降级永不匹配）；test-full-scan 26/0，全量 12 套件绿；狗粮本仓 lib/ 精确命中 6 处真残留- 2026-09-08 · 批次1（A1-A6 / B1-B6 / D7 / E2 部分）：core.js 同级仓模块整体废除（-13 符号），凭据/索引/要求清单/skill 注入四条链全部改插件自持 + 技能仓库；安全止血 6 项完成；全部 lib 语法绿，maskRemoteUrl/credentialsDir/repo-index/tools-index/token 解析冒烟 12/12 通过；template/user-repo 移除（git 历史可恢复）；技能仓库只读故 B3 落插件 skills/
- 2026-09-08 · 批次3（D1 全量 + E1）：**文件级拆分（用户指示：大单文件按功能拆小文件，行为零变化）**——core.js 2511 行拆 11 个功能模块（git-core/github-api/repo-scan/ignore-scan/commit-push/token-credentials/workspace-context/version-history/readme-gen/remote-repo/plugin-paths）+ 门面，导出面与拆分前逐名一致；index.js 巨型 apply（1183 行）拆 9 个 plugin-*.js 模块（config/setup/context-inject/repo-index-sync/audit/commit-flow/push-permit/http/tools），apply 只留调度，副作用注册顺序保持原序；repo-index.js/repo-index 内 buildRepoIndex 等超长函数拆步骤函数；**check-fnlen 对 index.js/core.js 清零**（原 blocker：apply 1183 / commitAndPush 214 / pushViaApi 112 / auditFile 213 / client.js 644 全消）；残留 10 项落在新增 plugin-* 的 register* 编排函数（内嵌工具/HTTP 闭包按嵌套计数）与 viewer.js w68（按指示留待重写）——均列批次4 处理；**顺带修复两雷**：v1.41.0 引入的 apply 启动 log TDZ 必崩（`const log` 提前）+ rebuildHistory squash/drop 分支提交无身份兜底（commitWithIdentityArgs 对齐 fresh 分支）；E1 test-core 全量重写（132 断言，tmp 隔离不再命中真实同级仓）+ test-apply 缺 runtime 优雅跳过；版本 1.42.0，README 版本表/目录表同步


- 2026-09-08 · 批次2（C1/C2/C3/C5/C6/C7 全量 + C4 部分）：规则包化落地——`lib/rule-packs.js`（装载/校验/编译三段，JSONC 注释剥离，非法正则降级）+ `lib/audit-rules/eightfs.rules.json`（EIGHTfs 全量规则 21 条 + 质量阈值）；audit.js 改纯引擎（secret/credential-file/credential-ref/wording/docConv 五桶全走规则包，initAuditRuleset/applyRulePack/getAuditRuleset 三入口，mergeWording 同名覆盖保住 commentWordingCustom 层）；JSON 注释豁免落地（json `//` 行豁免隐私入库类，JSONC 解析容忍）；auditRepo 输出 ruleset 溯源 + quality scoringBasis 诚实标注；index.js 接线 auditRuleset 配置/code_audit ruleset 参数/audit?ruleset=/status ruleset 字段；新增 test-rule-packs.mjs 34 断言全绿，全量测试零回归；C4 剩 4 个风格检查器（1.js 吸收）待批次3
