# 函数索引 · lib/score/index.js

共 2 个函数：

| 函数 | kind | 行 | 签名（前 60 字） |
|---|---|---|---|
| countByDimension | function | L42 | `export function countByDimension(findings = []) {` |
| scoreQuality | function | L71 | `export function scoreQuality(findings = [], weights = {}, co` |

## countByDimension（L42）

`export function countByDimension(findings = []) {`

（无注释，待补）


## scoreQuality（L71）

`export function scoreQuality(findings = [], weights = {}, context = {}) {`

（无注释，待补）


