# 审计豁免（不影响 git 跟踪，仅跳过审计扫描）
# 语法同 .gitignore；用于「要入库但不该被自家规则审」的内容。

# 第三方内置代码（vendor）：构建产物、非本项目编写。
#   已内置 js-yaml（MIT，dist/js-yaml.mjs）以实现零依赖发布。
#   其 parser 有 1370 行的单函数，会被 readability/max-function-length 报 blocker——
#   但我们不改这份代码，报出来只是噪声。
lib/vendor/

# 评分参数表：DEFAULT_WEIGHTS / DIMENSION_K 的数字是「维度名 → 参数」映射值，
#   键名已表意，拆成 WEIGHT_X = 15 这类常量反而割裂表格、降低可读性；
#   权重另由侧边栏配置项可调，不是散落魔数。故整文件豁免质量类规则。
lib/score/index.js
