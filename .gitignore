node_modules/
*.log
.DS_Store
# npm 下载产物（dsh-git-push 自动追加，2026-08-20）
package-lock.json
yarn.lock
pnpm-lock.yaml
bun.lock
npm-debug.log*
.npm/
.pnpm-store/

# 历史：User/ 曾嵌在插件目录（v1.18.0 起改同级仓 dsh-git-push-User，安装会清空插件内残留）
User/
User/**

# clone 通道元数据（本机专用，不入库）
.clone-meta.json
.git-push-token

# 未完成草稿（YAML 非法，暂不入库）
docs/code-quality-checklist.yaml
